on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Hello world action step
      id: hello
      uses: hello-world-purescript-action
      with:
        who-to-greet: 'PureScript User'
